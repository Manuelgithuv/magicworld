name: Codacy Analysis

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-codacy:
    name: Codacy Java Backend Analysis
    runs-on: ubuntu-latest
    env:
      CODACY_PROJECT_TOKEN: ${{ secrets.CODACY_PROJECT_TOKEN }}
    defaults:
      run:
        working-directory: ./
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
      - name: Build and run tests with JaCoCo
        run: >-
          mvn -B -Dspring.profiles.active=test -DskipTests=false
          org.jacoco:jacoco-maven-plugin:0.8.12:prepare-agent
          test
          org.jacoco:jacoco-maven-plugin:0.8.12:report
      - name: Upload coverage to Codacy (Java)
        if: env.CODACY_PROJECT_TOKEN != ''
        run: >-
          bash <(curl -Ls https://coverage.codacy.com/get.sh)
          report -l Java -r target/site/jacoco/jacoco.xml

  frontend-codacy:
    name: Codacy Angular Frontend Analysis
    runs-on: ubuntu-latest
    env:
      CODACY_PROJECT_TOKEN: ${{ secrets.CODACY_PROJECT_TOKEN }}
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'frontend/package-lock.json'
      - name: Setup Google Chrome
        uses: browser-actions/setup-chrome@v1
      - name: Expose CHROME_BIN env
        run: echo "CHROME_BIN=$CHROME_PATH" >> $GITHUB_ENV
      - name: Install dependencies
        run: npm ci
      - name: Run tests and generate coverage
        run: npm run test -- --code-coverage --watch=false --browsers=ChromeHeadless
        env:
          CI: true
      - name: Upload coverage to Codacy (Angular)
        if: env.CODACY_PROJECT_TOKEN != ''
        run: >-
          bash <(curl -Ls https://coverage.codacy.com/get.sh)
          report -l JavaScript -r coverage/frontend/lcov.info

  static-analysis:
    name: Codacy Static Analysis (Monorepo)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    env:
      CODACY_PROJECT_TOKEN: ${{ secrets.CODACY_PROJECT_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run Codacy Analysis CLI (Docker)
        if: env.CODACY_PROJECT_TOKEN != ''
        run: >-
          docker run --rm -v "$PWD":/src -w /src
          -e CODACY_PROJECT_TOKEN=$CODACY_PROJECT_TOKEN
          codacy/codacy-analysis-cli:latest
          analyze -d /src
          --format sarif --output /src/codacy.sarif
      - name: Upload SARIF to GitHub Code Scanning
        if: env.CODACY_PROJECT_TOKEN != '' && github.event_name == 'push'
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: codacy.sarif
